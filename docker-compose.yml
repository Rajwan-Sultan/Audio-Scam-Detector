version: '3.8'

services:
  scam-detector:
    build: .
    container_name: realtime-scam-detector
    ports:
      - "8000:8000"
    environment:
      - GOOGLE_API_KEY=${GOOGLE_API_KEY}
    restart: unless-stopped

networks:
  default:
    driver: bridge